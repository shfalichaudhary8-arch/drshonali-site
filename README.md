# Dr Shonali Website (Next.js App Router)

## Quick Start (No coding)
1. Create free accounts: GitHub + Vercel
2. On Vercel, **New Project → Import from GitHub** (upload this folder to a new repo first)
3. Set **Environment Variables** (optional now): none required
4. Deploy

### After Deploy
- Replace in `app/layout.tsx`: `G-XXXXXXX` (GA4), `YOUR_PIXEL_ID` (Meta Pixel)
- Replace in `app/page.tsx` inside `SITE`: emails, phone, social links, calendly, formspree
- Add your custom domain in Vercel: `drsonalichaudhary.com` & `drsonalichaudhary.in`
